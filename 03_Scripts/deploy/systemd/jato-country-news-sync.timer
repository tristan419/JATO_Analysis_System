[Unit]
Description=JATO Country News Sync daily timer

[Timer]
# Run at 06:15 local time every day (same as macOS launchd schedule).
OnCalendar=*-*-* 06:15:00
Persistent=true
RandomizedDelaySec=120

[Install]
WantedBy=timers.target
